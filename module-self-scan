	SELECT T.TABLE_SCHEMA + '.' + T.TABLE_NAME
	FROM INFORMATION_SCHEMA.ROUTINES AS R
		LEFT JOIN INFORMATION_SCHEMA.TABLES AS T ON CHARINDEX(T.TABLE_NAME, R.ROUTINE_DEFINITION) > 1
	WHERE R.ROUTINE_NAME = OBJECT_NAME(@@PROCID)
